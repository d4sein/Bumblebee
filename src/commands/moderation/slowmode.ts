import { CommandParams } from "../../commands.config";

module.exports = {
  run: async (params: CommandParams): Promise<void> => {
    //
  },
  name: 'slowmode',
  description: 'ff',
  usage: 'slowmode',
  category: 'Moderation'
}
